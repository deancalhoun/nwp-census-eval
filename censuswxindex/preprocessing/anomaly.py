import os
import glob
import xarray as xr
import numpy as np
import pandas as pd

def calculate_ifs_t2m_fc_anomaly(fc_dir, clim_path, save_dir, year, month, init_time, lead_time):
    '''
    
    '''
    return

def calculate_ifs_t2m_an_anomaly(an_dir, clim_path, save_dir, year, month, valid_time):
    '''
    
    '''
    return